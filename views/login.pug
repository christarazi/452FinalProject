html
	head
		title Login
	body
		h1=message

	form(method='post')
		label Username:
		input(name='username')
		br
		label Password:
		input(type='password', name='password')
		input(type='submit')
		input(type='hidden', name='_csrf', value=csrfToken)
